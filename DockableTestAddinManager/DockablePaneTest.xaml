<UserControl x:Class="DockableTestAddinManager.DockablePaneTest"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <StackPanel VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,20,0,0">
            <!-- Zone de texte -->
            <TextBlock Text="Dockable Pane Example"
                       FontSize="16" 
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"/>

            <!-- Bouton -->
            <Button Content="Add Debug Com"
                    Width="150"
                    Height="30"
                    HorizontalAlignment="Center" Click="AddText"/>
        </StackPanel>
    </Grid>
</UserControl>
